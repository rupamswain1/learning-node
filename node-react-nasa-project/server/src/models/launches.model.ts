type launchInterface{
    flightNumber:Number,
    mission:String,
    rocket:String,
    launchDate:Date,
    destination:String,
    customer:String[],
    upcoming:boolean,
    success:boolean
}

const launches:launchInterface[]=[
    
]

